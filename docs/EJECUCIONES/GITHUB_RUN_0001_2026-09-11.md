# Ejecución GitHub #1 — 2026-09-11

## Identidad
- GitHub Actions run: `34575702377`
- Workflow: `Procedimiento DDD — Aragón`
- Modo solicitado: `completo`
- Rama: `main`
- Commit ejecutado: `416b06dd1044acbad74ddc400d86ba2b99cf03f5`
- Workflow ejecutado: v2.3.0
- Configuración ejecutada: v7.1.0
- Resultado global: **FAIL de infraestructura previa a M01**

## Secuencia observada
1. Checkout del repositorio: PASS.
2. Instalación de PyYAML 6.0.2: PASS.
3. Cálculo de clave M01-M03: ERROR interno de PyYAML por sintaxis inválida en `io.cache.dir`.
4. El step no abortó porque el comando Python estaba embebido dentro de `echo`; se publicó de hecho una clave de caché vacía (`ddd-aragon-preparacion-`).
5. Restauración de caché: MISS, como consecuencia de la clave vacía y ausencia de preparación previa.
6. Materialización de fuentes territoriales: FAIL; `inputs/seccionado_2025.zip` e `inputs/65034.csv.zip` no estaban presentes en el remoto/LFS.
7. Construcción Docker: no ejecutada.
8. M01-M03: no ejecutados.
9. Job `ejecutar-distritacion`: omitido por dependencia fallida.
10. M04-M08 y validación algorítmica: no ejecutados.

## Error primario reproducido
`yaml.parser.ParserError` en la línea de configuración:

```yaml
cache: {dir: .cache/ddd/preparacion/{run_name}}
```

PyYAML interpretó las llaves de `{run_name}` como sintaxis YAML dentro del flow mapping. La ruta debía ser un escalar entrecomillado.

## Error de control detectado
El workflow v2.3.0 calculaba la clave mediante:

```bash
echo "key=$(python herramientas/calcular_clave_preparacion.py --params configuracion/aragon_2025.yaml)" >> "$GITHUB_OUTPUT"
```

El fallo del proceso Python quedó oculto por el exit status exitoso de `echo`, permitiendo continuar con `key` vacía. Esto se clasifica como defecto de fail-fast y trazabilidad.

## Segundo bloqueo observado
Al intentar materializar inputs territoriales:

```text
sha256sum: inputs/seccionado_2025.zip: No such file or directory
sha256sum: inputs/65034.csv.zip: No such file or directory
```

Este bloqueo es independiente del ParserError y permanece pendiente después de R005: los dos ZIP nacionales todavía no están disponibles físicamente para el runner.

## Acción correctiva R005
- Configuración 7.1.0 archivada en `legacy/configuracion/aragon_2025_v7.1.0.yaml`.
- Workflow 2.3.0 archivado en `legacy/workflows/procedimiento-ddd_v2.3.0.yml`.
- Configuración promovida a 7.1.1 con todas las rutas parametrizadas entrecomilladas.
- Workflow promovido a 2.3.1: captura la clave en una variable, exige un SHA-256 hexadecimal de 64 caracteres y aborta con código 21 si es inválida.
- El bloqueo de inputs territoriales queda explícitamente separado para la siguiente ronda/ejecución.

## Valor probatorio
Esta ejecución no aporta evidencia sobre calidad geográfica o algorítmica. Sí aporta evidencia válida sobre el funcionamiento del disparador manual de GitHub Actions y sobre dos fallos de infraestructura detectados antes de M01.
